<odoo>

    <!-- ♦ PDF Template ♦ -->
    <template id="cash_book_pdf">
        <main/>
        <t t-call="web.basic_layout">
            <script>
                function resizeHandler() {
                    // Treat each container separately
                    $(".container").each(function(i, container) {
                        // Stores the highest rowheight for all tables in this container, per row
                        var aRowHeights = [];
                        // Loop through the tables
                        $(container).find("table").each(function(indx, table) {
                        // Loop through the rows of current table (clear their css height value)
                            $(table).find("tr").css("height", "").each(function(i, tr) {
                            // If there is already a row height defined
                                if (aRowHeights[i])
                                // Replace value with height of current row if current row is higher.
                                aRowHeights[i] = Math.max(aRowHeights[i], $(tr).height());
                                else
                                // Else set it to the height of the current row
                                aRowHeights[i] = $(tr).height();
                            });
                        });
                        // Loop through the tables in this container separately again
                        $(container).find("table").each(function(i, table) {
                            // Set the height of each row to the stored greatest height.
                            $(table).find("tr").each(function(i, tr) {
                                $(tr).css("height", aRowHeights[i]);
                            });
                        });
                    });
                }

                $(document).ready(resizeHandler);
                $(window).resize(resizeHandler);
            </script>
            <t t-raw="report"/>
            <style>
                td{
                    height:24px !important
                }
            </style>

        </t>
    </template>

    <!-- ♦ Paper Format ♦ -->
    <record id="cash_book_pdf_paper_format" model="report.paperformat">
        <field name="name">Cash Book Js Paper Format</field>
        <field name="default" eval="True"/>
        <field name="format">A4</field>
        <field name="page_height">0</field>
        <field name="page_width">0</field>
        <field name="orientation">Portrait</field>
        <field name="margin_top">5</field>
        <field name="margin_bottom">5</field>
        <field name="margin_left">0</field>
        <field name="margin_right">0</field>
        <field name="header_line" eval="False"/>
        <field name="header_spacing">28</field>
        <field name="dpi">109</field>
    </record>

    <!-- ♦Action ♦ -->
    <report id="cash_book_pdf_action"
            model="cash.book.pdf"
            string="Cash Book"
            report_type="qweb-pdf"
            name="cash_book.cash_book_pdf"
            file="cash_book.cash_book_pdf"
            paperformat="cash_book.cash_book_pdf_paper_format"
            menu="False"/>

</odoo>